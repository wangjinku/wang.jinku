1 查询kona000表家庭住址在大唐或武当山的人的姓名和电话
select name,phone from kona000 where  address in('大唐','武当山');
2 查询kona000表中名字含张字，且住在武当山的全部信息
select * from kona000 where address='武当山' and name like ('%张%');

3 查询kona000表ID不在3/5/7/9/11/15/17的名字  住址  性别
select name,address,sex from kona000 where ID not in(3,5,7,9,11,15,17);
4查询kona000表年龄在25到36之间的人的姓名、性别.（在…之间不包含25和36）
select name,sex from kona000 where age>25 and age<36;
5查询kona000表id在5到10且年龄小于30的名字、性别、和住址
 select name,sex,address form kona000 where between 5 and 10 and age<30;

6查询kona000表年龄大于30且家庭住址不在武当山和峨眉山的全部信息
 select * from kona000 where age>30 and address not in('武当山'，'峨眉山')；
7查询kona000表去除重复年龄
 select distinct age from kona000;
8查询kona000表三个字的名字，且年龄大于25的人的名字
select name from kona000 where age>25 and name like'___';
9查询kona000表id为20,31,19,5的名字和住址
select name,address from kona000 where ID in(20 , 31 ,19 ,5);
1 查询出kona表中姓张的姓名  电话 
Select name,phone from kona where name like ('张%')
2查询出不是峨眉山的所有人的信息
 Select * from kona  where  home！=('峨眉山')
3 查询出名字中大于三字符的信息
Select * from kona  where name=('%____%')
4查询出张三的性别和住址
Select sex ,home from kona  where name = '张三'
5查询出名字是两个字的人员年龄
Select age from kona  where name like'__'



查询出家在武当山的所有人的总年龄
select sum(age) from 表名 where address=('武当山'); 

查询出加不在武当山的所有的总年龄
select sum(age) from 表名 where address not in('武当山')；

对年龄进行分组，并求出每个年龄的总年龄 
select sum(age) from 表名 having group by age;

求出每个家庭的平均年龄
select avg(age) from 表名 having group by address;

查询出每个家庭中年龄大于20的人员的所有信息
select * from 表名 where age>20;

统计每个家庭中的男生人数
select sum('男') from 表名having group by address ；

查询出男生的最小年龄
select min(age) from 表名 where sex=('男')；

查询出女生的最大年龄
select max(age) from 表名 where sex=('女')；

       ！！！！！！！！  两表联合！！！

.查询出姓名中大于三个字的男孩的总成绩
select sum(fenshu) from student_kona inner join score on student_kona.id=score.student_id where name like('%____%') and sex='男';

查询出姓名中带张字的总成绩最小的人


显示每个人的成绩与年龄
select fenshu,age from (select * from student_kona inner join score on student_kona.id=score.student_id ) as a;

显示张三丰的所有成绩
select * from student_kona inner join score on student_kona.id=score.student_id where name='张三丰';

求出所有男生的总成绩
select sum(fenshu) from student_kona inner join score on student_kona.id=score.student_id where sex='男';

显示家在武当山的所有人的成绩
select * from student_kona inner join score on student_kona.id=score.student_id where address='武当山';

显示所有女生的成绩
select * from student_kona inner join score on student_kona.id=score.student_id where sex='女';

显示每个的男生的总成绩

select * from student_kona inner join score on student_kona.id=score.student_id having group by id sex='男'

Select sum(fenshu),id,sex from student_kona a inner join score b on a.id=b.student_id group by id having sex='男';